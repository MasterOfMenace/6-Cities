language: node_js

node_js:
  - "10"

cache:
  directories:
    "node_modules"

script:
  - npm test
  - npm run build

deploy:
  provider: pages
  github-token: $GH_TOKEN
  cleanup: true
  keep-history: true
  on:
    branch: master
